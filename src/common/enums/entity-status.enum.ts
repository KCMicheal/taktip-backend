// Numeric enum for entity status
// 1 = ACTIVE, 2 = INACTIVE, 3 = PENDING, 4 = SUSPENDED, 5 = DELETED
export enum EntityStatus {
  ACTIVE = 1,
  INACTIVE = 2,
  PENDING = 3,
  SUSPENDED = 4,
  DELETED = 5,
}