// Numeric enum for shift lifecycle status
// 1 = DRAFT, 2 = PUBLISHED, 3 = IN_PROGRESS, 4 = COMPLETED, 5 = CANCELLED
export enum ShiftStatus {
  DRAFT = 1,
  PUBLISHED = 2,
  IN_PROGRESS = 3,
  COMPLETED = 4,
  CANCELLED = 5,
}
