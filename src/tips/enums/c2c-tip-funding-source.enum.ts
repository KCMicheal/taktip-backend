// Numeric enum for C2C tip funding source
// 1 = WALLET (funded from customer wallet balance)
// 2 = CARD (funded via Paystack card payment)
export enum C2cTipFundingSource {
  WALLET = 1,
  CARD = 2,
}
