name: Test CI

on:
  push:
    branches: [test-ci]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Test
        run: |
          echo "Test workflow running"
          echo "Node version:"
          node --version
